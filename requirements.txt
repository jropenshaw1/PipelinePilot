# PipelinePilot — Python Dependencies
# Python 3.10+

# GUI
customtkinter>=5.2.0

# Document generation
python-docx>=1.1.0
lxml>=4.9.0          # Required by python-docx for XML manipulation

# YAML front-matter parsing
PyYAML>=6.0.1

# Claude API — fit analysis engine
anthropic>=0.40.0

# OpenBrain bridge — Supabase REST API
requests>=2.31.0

# Standard library (no install needed on Windows — listed for documentation)
# copy
# datetime
# json
# os
# pathlib
# re
# shutil
# sqlite3
# threading
# tkinter    ← bundled with Python on Windows; Linux requires: sudo apt install python3-tk
